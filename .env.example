# Elastic Cloud — use either (A) direct URL + API key or (B) Cloud ID + API key
# (A) Direct URL (e.g. https://my-deployment.es.region.gcp.elastic.cloud:443)
ELASTIC_URL=
# (B) Cloud ID (alternative to ELASTIC_URL). Format: ID-ProjectName:base64string
# Example: ID-My_Elasticsearch_project:dXMtY2VudHJhbDEu...
ELASTIC_CLOUD_ID=

# API key (base64-encoded id:secret from Elastic Cloud console)
ELASTIC_API_KEY=

# Kibana base URL for deep links and Create Case API (no trailing slash)
KIBANA_URL=
# Optional: Kibana space (default)
ELASTIC_SPACE_ID=default

# Optional: embedding model
EMBEDDING_MODEL=all-MiniLM-L6-v2

# LLM for copilot (propose_fix, runbook). Set one provider and its key.
# Options: groq | openrouter | mistral | cerebras
LLM_PROVIDER=
OPENAI_API_KEY=
GROQ_API_KEY=
CEREBRAS_API_KEY=
OPENROUTER_API_KEY=
MISTRAL_API_KEY=

# Demo login (required for /debug)
DEMO_USER=demo
DEMO_PASSWORD=demo123
JWT_SECRET_KEY=your-secret-key-change-in-production

# Google (e.g. Gemini for LLM). Store in .env only; never commit.
# GOOGLE_API_KEY=

# Stitch (with Google) – for MCP / design tools. See docs/STITCH_MCP.md.
# STITCH_API_KEY=
